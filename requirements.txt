# Root-level requirements.txt for Streamlit Community Cloud.
#
# Community Cloud runs Debian Linux and installs this file directly with
# pip. The --extra-index-url below points pip at the official CPU-only
# PyTorch wheel index so it resolves the "+cpu" build instead of a CUDA
# build (CUDA wheels are much larger and entirely unnecessary here — this
# project never uses a GPU in production).
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu
torchaudio==2.11.0+cpu
transformers==5.15.1
huggingface_hub==1.28.0
safetensors==0.8.0
numpy==2.5.2
scipy==1.18.1
soundfile==0.14.0
librosa==1.0.0
pydantic==2.13.4
pydantic-settings==2.15.0
PyYAML==6.0.3
psutil==7.2.2
streamlit==1.62.0
matplotlib==3.11.1
